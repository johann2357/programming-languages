angular.module('app').
    controller('TweetCtrl', [
        '$scope', '$rootScope', '$timeout',
        function ($scope, $rootScope, $timeout) {
            $scope.init = function () {
            }
            $scope.init();
        }
    ]
);
