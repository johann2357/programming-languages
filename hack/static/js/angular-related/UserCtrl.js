angular.module('app').
    controller('UserCtrl', [
        '$scope', '$rootScope', '$timeout',
        function ($scope, $rootScope, $timeout) {
            $scope.init = function () {
            }
            $scope.init();
        }
    ]
);
