angular.module('app').
    controller('SessionCtrl', [
        '$scope', '$rootScope', '$timeout',
        function ($scope, $rootScope, $timeout) {
            $scope.init = function () {
            }
            $scope.init();
        }
    ]
);
