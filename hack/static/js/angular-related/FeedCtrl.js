angular.module('app').
    controller('FeedCtrl', [
        '$scope', '$rootScope', '$timeout',
        function ($scope, $rootScope, $timeout) {
            $scope.init = function () {
            }
            $scope.init();
        }
    ]
);
